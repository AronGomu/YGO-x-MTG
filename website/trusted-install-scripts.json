{
  "schemaVersion": 1,
  "reviewedBy": "AronGomu",
  "packages": {
    "esbuild": "Downloads/verifies platform-specific compiler binary required by Astro/Vite.",
    "sharp": "Verifies platform-specific libvips binary used only by build-time image generation.",
    "fsevents": "Optional macOS filesystem watcher native rebuild; never shipped to browser or Linux Pages runtime."
  }
}
